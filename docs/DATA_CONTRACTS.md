# Data Contracts

Capture payload and schema contracts between workflows and external systems.

## Contract Template
### Contract Name
- Producer workflow/node:
- Consumer workflow/node:
- Transport: (n8n item JSON / webhook body / DB row / API payload)
- Required fields:
  - `field_name` (`type`) - description
- Optional fields:
  - `field_name` (`type`) - description
- Validation rules:
  - 
- Failure handling:
  - 

## Notes
- Update this whenever payload shape changes.
- Keep examples sanitized (no secrets/PII).
